name: Skill Drift

on:
  pull_request:
    branches: [testing]

permissions:
  contents: read
  pull-requests: read

jobs:
  skill-drift:
    # Guardrail: CI/workflow changes must update the matching skill and docs in the same PR.
    uses: projectbluefin/actions/.github/workflows/skill-drift-check.yml@38dc24b919e557a1204e26641887af792bf6cbad # v1
    with:
      code-paths: '[".github/workflows/**", ".github/actions/**", "build_files/**", "Justfile", "recipes/**"]'
      skill-paths: '["docs/skills/**", "docs/*.md", "AGENTS.md"]'
