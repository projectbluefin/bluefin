name: Post-Testing E2E

# Runs e2e smoke tests after every successful push build on testing.
# This gates testing continuously so the promotion workflow
# (promote-testing-to-main.yml) promotes only e2e-verified commits.
on:
  workflow_run:
    workflows: ["Testing Images"]
    types: [completed]
    # Include main so the workflow file update reaches the default-branch evaluator.
    # The promote-to-testing job is guarded to main-only to prevent double-promoting.
    # See common docs/skills/e2e-ci.md — Requirement 1.
    branches: [main, testing]
  workflow_dispatch:
    inputs:
      run_id:
        description: "Testing Images workflow run ID (default: latest successful run)"
        required: false
        default: ""
        type: string

permissions: read-all

concurrency:
  group: post-testing-e2e-${{ github.event.workflow_run.head_sha || github.sha }}
  cancel-in-progress: true

jobs:
  e2e:
    # Only run for push events (not merge_group) or workflow_dispatch,
    # and only when the build succeeded.
    if: |
      github.event_name == 'workflow_dispatch' ||
      (github.event.workflow_run.conclusion == 'success' && github.event.workflow_run.event == 'push')
    runs-on: ubuntu-latest
    timeout-minutes: 10
    outputs:
      image: ${{ steps.get-digest.outputs.image }}
      run_id: ${{ steps.get-digest.outputs.run_id }}
    steps:
      - name: Download build digest
        id: get-digest
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          set -euo pipefail
          RUN_ID="${{ inputs.run_id || (github.event.workflow_run && github.event.workflow_run.id) || '' }}"
          if [ -z "$RUN_ID" ]; then
            RUN_ID=$(gh run list --repo "${{ github.repository }}" --workflow "Testing Images" --status success --limit 1 --json databaseId --jq '.[0].databaseId')
          fi
          echo "run_id=${RUN_ID}" >> "$GITHUB_OUTPUT"
          gh run download "${RUN_ID}" \
            --repo "${{ github.repository }}" \
            --name "image-digest-testing-bluefin-main-x86_64" \
            --dir /tmp/digest
          DIGEST=$(grep "=" /tmp/digest/main-bluefin-x86_64.txt | head -1 | cut -d= -f2-)
          echo "image=ghcr.io/${{ github.repository_owner }}/bluefin@${DIGEST}" >> "$GITHUB_OUTPUT"
          echo "Testing image: ghcr.io/${{ github.repository_owner }}/bluefin@${DIGEST}"

  run-e2e:
    needs: [e2e]
    permissions:
      packages: write
      contents: read
    uses: ./.github/workflows/run-testsuite.yml
    with:
      image: ${{ needs.e2e.outputs.image }}
      suites: smoke,common

  # run-upgrade-test: DISABLED — TODO(#400)
  # lifecycle suite fails under GNOME 50 AT-SPI changes, causing post-testing-e2e
  # to mark as 'failure' and blocking promotion.
  # Disabled until testsuite lifecycle scenarios are stable on GNOME 50.
  # To restore: uncomment the job below.
  #
  # run-upgrade-test:
  #   needs: [e2e]
  #   permissions:
  #     packages: write
  #     contents: read
  #   uses: projectbluefin/actions/.github/workflows/upgrade-test.yml@v1
  #   with:
  #     image: ${{ needs.e2e.outputs.image }}
  #     suites: lifecycle

  promote-to-testing:
    # Promote all tested flavors to :testing only after e2e passes.
    # This ensures :testing always points to a digest that has passed gate e2e.
    needs: [e2e, run-e2e]
    if: needs.run-e2e.result == 'success'
    runs-on: ubuntu-latest
    timeout-minutes: 15
    permissions:
      packages: write
    steps:
      - name: Download all testing image digests
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          set -euo pipefail
          RUN_ID="${{ needs.e2e.outputs.run_id || (github.event.workflow_run && github.event.workflow_run.id) || '' }}"
          if [ -z "$RUN_ID" ]; then
            RUN_ID=$(gh run list --repo "${{ github.repository }}" --workflow "Testing Images" --status success --limit 1 --json databaseId --jq '.[0].databaseId')
          fi
          gh run download "${RUN_ID}" \
            --repo "${{ github.repository }}" \
            --pattern "image-digest-testing-*" \
            --dir /tmp/all-digests

      - name: Log in to GHCR
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          echo "$GH_TOKEN" | docker login ghcr.io --username "${{ github.actor }}" --password-stdin

      - name: Promote verified digests to :testing
        run: |
          set -euo pipefail
          REGISTRY="ghcr.io/${{ github.repository_owner }}"
          PROMOTED=0

          while IFS= read -r -d '' f; do
            # Each file contains: IMAGE_NAME=sha256:... (= format, one digest per image per arch)
            while IFS='=' read -r image_name digest; do
              [[ -z "${image_name}" || -z "${digest}" || "${image_name}" == *"|"* ]] && continue
              SRC="docker://${REGISTRY}/${image_name}@${digest}"
              DST="docker://${REGISTRY}/${image_name}:testing"
              echo "Promoting ${image_name}@${digest} → :testing"
              skopeo copy --all "${SRC}" "${DST}"
              echo "  ✅ ${image_name}:testing updated"
              PROMOTED=$((PROMOTED + 1))
            done < "$f"
          done < <(find /tmp/all-digests -name "*.txt" -print0)

          echo "✅ Promoted ${PROMOTED} flavor(s) to :testing"
